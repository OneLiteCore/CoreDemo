<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <core.demo.frag.dijkstra.MapView
        android:id="@+id/mapView_frag_map"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <RadioGroup
        android:id="@+id/radioGroup_frag_map"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@drawable/core_bg_gradient_up_10"
        android:orientation="horizontal"
        android:visibility="gone">

        <RadioButton
            android:id="@+id/radioButton_frag_map_first"
            style="@style/FloorBtnStyle"
            tools:text="一层"/>

        <RadioButton
            android:id="@+id/radioButton_frag_map_second"
            style="@style/FloorBtnStyle"
            tools:text="二层"/>

    </RadioGroup>

</FrameLayout>